name: Publish release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  publish-release:
    name: Publish release
    uses: NorthRealm/SocksProxyClientOfMinecraft/.github/workflows/release.yml@master
    with:
      tag-ref: ${{ github.ref }}
      java-version: "21"
      java-distribution: "oracle"
    secrets:
      MODRINTH_TOKEN: ${{ secrets.MODRINTH_TOKEN }}
